[project]
name = "rag-chatbot"
version = "1.0.0"
description = "Adaptive + corrective RAG chatbot over Postgres/pgvector"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"
